{
  "dependencies": {
    "@safe-global/protocol-kit": "^1.18.0",       // Safe Core SDK
    "@safe-global/safe-core-sdk-types": "^1.13.0", // Types for Safe SDK
    "@safe-global/safe-ethers-adapters": "^1.18.0", // For Ethers.js compatibility
    "ethers": "^5.7.2" // Or ^6.x if you're using Ethers v6 (Safe SDK works best with v5)
  }
}
