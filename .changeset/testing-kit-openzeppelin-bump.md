---
'@safe-global/testing-kit': patch
---

Bump the `@openzeppelin/contracts` dependency used by the test fixtures and inline the `ERC20Mintable` mock previously pulled from `@openzeppelin/contracts`. The Safe 1.2.0 singleton address used in tests was updated accordingly.
